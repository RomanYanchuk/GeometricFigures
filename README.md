# GeometricFigures - Local Configs

## Db

- Start Docker;
- Run `cd ./db/local`;
- Run `docker-compose -f environment.yml up -d`;
- Run `update-database` in PMC.

## Back-end

- Run `cd backend/GeometricFigures/GeometricFigures`;
- Run `dotnet run`.
